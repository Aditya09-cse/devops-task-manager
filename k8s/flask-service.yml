apiVersion: v1
kind: Service
metadata:
  name: flask-service
  namespace: aditya-namespace

spec:
  selector:
    app: flask-task-manager

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: ClusterIP

